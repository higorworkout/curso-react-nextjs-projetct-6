import * as Styled from './styles';

export type FooterProps = {
  title: string;
};

export const Footer = ({ title = '' } :FooterProps) => {
  return (
    <Styled.Container>
    </Styled.Container>
  );
};


